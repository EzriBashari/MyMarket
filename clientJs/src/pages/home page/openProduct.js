import React from 'react'

const openProduct = ({ product, isOpen, openProduct, closeProduct }) => {
  const { Image, name, description, price } = product;
  return <div>openProduct</div>;
};

export default openProduct